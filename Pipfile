[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[scripts]
test = "pytest ."
format = "black ."

[packages]

[dev-packages]
black = "*"
pytest = "*"

[requires]
python_version = "3.8"
